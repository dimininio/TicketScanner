#ifndef MAINWINDOWWRAPPER
#define MAINWINDOWWRAPPER
/*
#include "windowtitlebar.h"

class Train;
class WidgetsMediator;
class QVBoxLayout;


class UZMainWindow: public QWidget
{
    Q_OBJECT
public:
    explicit UZMainWindow(QWidget *parent = 0);
    ~UZMainWindow();

    void setCentralWidget(QWidget* w);
    void showAvailableTrains();
    void showAvailableCoaches(Train* train);

private:

    WidgetsMediator* widgetsMediator;
    WindowTitleBar* titleBar;
    QWidget* bodyWindowWidget;
    QHBoxLayout* innerLayout;
    QVBoxLayout* mainLayout;

};*/

#endif // MAINWINDOWWRAPPER

